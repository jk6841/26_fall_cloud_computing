import type { MapViewProps } from "../types";

// Not yet implemented — wire up when MAP_PROVIDER=naver is selected.
// SDK: https://navermaps.github.io/maps.js.ncp/docs/
export function NaverMap(_props: MapViewProps) {
  return <div>Naver Map provider not implemented yet.</div>;
}
